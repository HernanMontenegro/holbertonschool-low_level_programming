#include "holberton.h"

/**
 * _strlen - count the amount of chars in an array
 * @s: the direction array of chars given
 * ------------------------------------
 * Return: the count of ints
 */
int _strlen(char *s)
{
int i, count;

for (i = 0; i <= s[i]; i++)
{
count++;
}
return (count);
}
